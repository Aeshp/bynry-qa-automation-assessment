[pytest]
addopts = --screenshot=only-on-failure --video=retain-on-failure --tracing=retain-on-failure --html=reports/report.html --self-contained-html
testpaths = tests
markers =
    smoke: quick sanity checks
    regression: full regression suite
    api: API-only tests
    mobile: mobile/BrowserStack tests